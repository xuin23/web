server:
  port: 8000
  servlet:
    context-path: /@pom.artifactId@
spring:
  application:
    name: @pom.artifactId@
  #mysql数据源
  datasource:
    url: jdbc:postgresql://xuin23.com:5432/web
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 123456
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: debug