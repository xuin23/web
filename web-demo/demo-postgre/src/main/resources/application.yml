server:
  port: 8000
  servlet:
    context-path: /@pom.artifactId@
spring:
  application:
    name: @pom.artifactId@
  #redis 数据源
  redis:
    database: 0
    port: 6379
    host: localhost
  #mysql数据源
  datasource:
    url: jdbc:postgresql://localhost:5432/web?search_path=web
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 123456

mybatis:
  type-aliases-package: com.cloud.demo.entity
  mapper-locations: classpath:mapper/*xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      cloud:
        demo:
          dao: debug